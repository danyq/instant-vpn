These are some scripts for quickly configuring a server as a VPN. They're very brittle and are only meant to be run once on a brand new Ubuntu 14.04 LTS server, with nothing else configured. For any other situation, just consider this a convenient collection of commands to reference.
